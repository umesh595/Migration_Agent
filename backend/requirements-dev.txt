-r requirements.txt

# Pinned to the versions this codebase was actually verified against.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
asgi-lifespan==2.1.0
ruff==0.16.2

# scripts/refresh_aws_pricing.py only — not imported by the app itself.
ijson==3.5.1
