# Copy to .env.local for local `npm run dev`. In docker-compose, NEXT_PUBLIC_API_BASE_URL
# is baked in at build time (see Dockerfile) since it must resolve from the *browser*,
# not from inside the web container — the browser can't reach the api service by its
# Docker network name.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
